export interface TextBox {
    textBox: string;
    textBoxBackground: boolean;
    fullText: boolean;
  }
export interface BoxImageText {
    image: string;
    textBox: TextBox;
    size: string;
    altText: string;
    variation: string;
  }
