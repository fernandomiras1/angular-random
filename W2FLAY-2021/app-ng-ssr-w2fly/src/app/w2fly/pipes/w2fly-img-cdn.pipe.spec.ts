import { W2flyImgCdnPipe } from './w2fly-img-cdn.pipe'

describe('W2flyImgCdnPipe', () => {
  it('create an instance', () => {
    const pipe = new W2flyImgCdnPipe()
    expect(pipe).toBeTruthy()
  })
})
