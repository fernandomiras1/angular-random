import { W2flyDateFormatPipe } from './w2fly-date-format.pipe'

describe('W2flyDateFormatPipe', () => {
  it('create an instance', () => {
    const pipe = new W2flyDateFormatPipe()
    expect(pipe).toBeTruthy()
  })
})
