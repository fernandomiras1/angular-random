import { W2flyTruncatePipe } from './w2fly-truncate.pipe'

describe('W2flyTruncatePipe', () => {
  it('create an instance', () => {
    const pipe = new W2flyTruncatePipe()
    expect(pipe).toBeTruthy()
  })
})
