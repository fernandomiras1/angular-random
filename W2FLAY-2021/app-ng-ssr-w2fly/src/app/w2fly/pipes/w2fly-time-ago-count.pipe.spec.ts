import { W2flyTimeAgoCountPipe } from './w2fly-time-ago-count.pipe'

describe('W2flyTimeAgoCountPipe', () => {
  it('create an instance', () => {
    const pipe = new W2flyTimeAgoCountPipe()
    expect(pipe).toBeTruthy()
  })
})
