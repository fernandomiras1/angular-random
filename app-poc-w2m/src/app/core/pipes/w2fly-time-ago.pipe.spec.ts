import { W2flyTimeAgoPipe } from './w2fly-time-ago.pipe'

describe('W2flyTimeAgoPipe', () => {
  it('create an instance', () => {
    const pipe = new W2flyTimeAgoPipe()
    expect(pipe).toBeTruthy()
  })
})
